# dotfiles
my dotfiles

For Neovim:

- Install the vim plug plug-in manager
- Create ~/.config/nvim folder and create a symbolic link to the init.lua from this repository
- :PlugInstall

For zsh:

- Create a symbolic link to the .zshrc from this repository in home directory
